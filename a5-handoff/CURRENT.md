# 当前交接任务

- 任务 ID：`A5-0-001`
- 更新时间：2026-09-17
- 状态：待内网执行，未验收
- 当前门禁：A5-0 环境冻结，先确认执行位置与资源
- 前置证据：尚未收到 A5 实测结果

## 目标

确认后续训练将使用的实际容器/Python 环境、A5 设备健康与占用，以及配套版本和文件准备情况。本轮不安装依赖、不启动 NPU 计算或训练。

## 执行位置

在已经获准使用的训练容器或环境中执行。若只有宿主机，就在宿主机采集并明确标注；不要自行创建、重启容器或激活猜测的环境。记录当前属于宿主机/容器、是否已激活 venv/Conda。

## 唯一执行步骤

在 Linux Bash 中逐条执行以下命令。通过内网终端的本地记录功能保存输出；不要把未脱敏记录提交到本仓。

```bash
uname -m
printf 'uname_exit_code=%s\n' "$?"

command -v python
printf 'python_path_exit_code=%s\n' "$?"

python --version
printf 'python_version_exit_code=%s\n' "$?"

npu-smi info
printf 'npu_smi_exit_code=%s\n' "$?"
```

这些是相互独立的只读诊断，某条失败仍可采集其余命令；不得为消除错误而安装软件或修改环境。设备忙时只报告，不清理或终止任何进程。

同时人工补充以下事实，不知道就写 `UNKNOWN`：

- 是否有 A5 内部验证过的版本组：CANN、torch、torch_npu、Triton-Ascend 的版本和配套构建批次。不得提供内部镜像/包源地址。
- TorchTitan 是否已准备好，实际位置是否为独立 clone/worktree；路径返回前脱敏。
- `a5_preflight.sh`、`a5_hccl_smoke.py`、`a5_flex_smoke.py` 是否已经传入内网，目录位置是否已知。
- 本轮是否在后续训练的实际环境执行；若不是，缺少什么访问条件。

## 注意

- 本轮不要运行现有 `a5_preflight.sh`：已检查的版本包含 NPU tensor 分配，不是纯信息采集，而且整体退出码 0 不代表所有检查通过。
- 不输出环境变量全集、pip 配置或内部访问地址。
- 不执行 HCCL、FlexAttention、训练、profiler 或任何兼容补丁。

## 回传与验收

用户可以手动执行并口述，不要求传回文件或填写模板。当前优先只反馈三项：

- 当前在宿主机还是训练容器，是否已进入计划使用的 Python 环境。
- Python 版本。
- `npu-smi info` 能否正常执行，显示的芯片型号、卡数、健康状态及是否有人占用。

命令失败时口述失败命令与错误关键词；不知道的写 UNKNOWN。其余版本组和文件准备情况可以在教师判断后逐项补问，不要求一次报全。

教师按 [RESULT-TEMPLATE.md](RESULT-TEMPLATE.md) 整理记录，任务 ID 为 `A5-0-001`，证据来源标记为用户口述；尚未报告的项目保持待确认。

本步骤 PASS 仅表示上述诊断均成功、执行位置明确且补充项已如实填写，不表示依赖配套或环境部署已通过。失败命令必须保留各自退出码；只有宿主机信息时明确说明，不能当作训练容器验收。

教师 agent 收到结果后，决定下一份环境采集任务；依赖关系不明确时停在 A5-0，不试错混装。执行完立即停止。
