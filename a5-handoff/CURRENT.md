# 当前交接任务

- 任务 ID：`A5-ENV-002`
- 更新时间：2026-09-17
- 状态：待内网执行，未验收
- 当前门禁：同事容器环境诊断与单卡组件检查
- 前置证据：用户口述宿主机 npu-smi、docker ps 正常；已借用同事初步搭建的容器。外网未核对原始日志。
- 代码目标：TorchTitan v0.3.0；A5 配套矩阵和模型代码适配仍待确认。

## 目标

使用一键脚本确认容器软件版本、加载路径，并在获准的空闲单卡上依次验证 NPU tensor、HCCL 和原生 FlexAttention。用户已要求完整环境诊断；本轮不安装或修改依赖、不启动模型训练。

## 执行位置

在同事借出的容器内、准备用于训练的 Python 环境执行，不在宿主机启动测试。先向同事确认授权卡组、可见设备映射和空闲状态，不创建或重启容器，不动他人进程。

## 唯一执行步骤

将 `a5_doctor.py` 转入内网。在脚本所在目录执行以下命令；只有确认逻辑 0 卡获准使用、空闲且测试期间独占时才使用该编号，否则换为实际获准的逻辑编号：

```bash
python a5_doctor.py --compute --device 0 --confirm-idle
```

不能确认设备空闲时执行 `python a5_doctor.py`，只做信息和导入检查。已有源码仓库时可加 `--repo /实际路径/torchtitan`。完整选项、退出码和范围见 [DOCTOR.md](DOCTOR.md)。

## 注意

- 脚本不联网、不安装、不卸载、不预打补丁；测试和编译会使用指定 NPU 和 CPU，并生成本地日志/缓存，不是零开销操作。
- 计算阶段前项失败即跳过后项；不要为消除错误自行改同事环境。
- 不执行四卡训练、profiler 或任何兼容补丁。测试小形状通过不代表 Kimi-K3 全模型通过。

## 回传与验收

用户只需口述，不要求传回文件或填写模板：

- torch、torch-npu、triton、triton-ascend 版本。
- OVERALL 和 tensor/hccl/flex 的 PASS/FAIL/SKIP。
- 若失败，FIRST_FAILURE 及对应日志里首个错误关键词。

所有完整日志保留在内网。需要进一步判断时再定向询问 CANN、驱动、设备映射、版本来源或错误上下文，不要求一次口述全部内容。

教师按 [RESULT-TEMPLATE.md](RESULT-TEMPLATE.md) 整理记录，任务 ID 为 `A5-ENV-002`，证据来源标记为用户口述；尚未报告的项目保持待确认。

即使 PASS_COMPONENT_SMOKE，也只表示本次小型组件检查通过。torch 2.14 的 A5 配套、同事环境是否带私有补丁、v0.3.0 代码门和单卡/四卡模型训练仍需单独验收。

教师收到结果后决定下一任务；版本关系不明确时不试错混装。执行完停止，不自动进入训练。
